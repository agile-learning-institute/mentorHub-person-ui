const state = {
    admin: {}, // This object will contain admin information
  };
  
  const mutations = {
    // Define mutations to add, update, and delete products
  };
  
  const actions = {
    // Define actions for making API requests and committing mutations
      
  };
  
  const getters = {
    // Define getters to access product data
  };
  
  export default {
    state,
    mutations,
    actions,
    getters,
  };
  